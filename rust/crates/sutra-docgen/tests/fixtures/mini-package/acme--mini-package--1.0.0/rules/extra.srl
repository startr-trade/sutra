// Fixture .srl file — the rules DSL; sutra-docgen renders a real page via sutra-srl.
rule "extra check"
  salience 5
  activation-group "balance"
when
  balance < 0
then
  set(status, "rejected");
  report("LOW_BALANCE", "balance", "balance is negative");
end
