{{!-- generated-by: sutra create bpmn %%PROCESS%% (edit freely — this file is yours) --}}
{{!-- Accepted reply for the %%PROCESS%% process. The decoded inbound message is available as
   `payload` (navigate its fields, e.g. {{payload.someField}}). --}}
{{!-- The reply carries the DEPLOYMENT NAMESPACE, and `Accepted` is declared in this
   deployment's schema alongside the inbound message. An unqualified answer would be a
   different element from the one the schema declares — the same trap the inbound side warns
   about — and a caller that validates what it receives could not check this at all. --}}
<Accepted xmlns="%%NAMESPACE%%" process="%%PROCESS%%"/>
