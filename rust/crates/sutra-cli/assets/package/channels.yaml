# generated-by: sutra create (edit freely — this file is yours)
#
# Channel declarations for this deployment package. A channel binds a transport endpoint to the
# packaged processes: the engine decodes the inbound bytes with the channel's codec and routes
# the message to the start event whose <q:source> subscribes to (channel, messageType).
#
# Every field below is documented on the commented sample. Uncomment and adapt.

channels: []

#channels:
#  - name: my-channel                         # channel id — <q:source channel="my-channel"/>
#    transport: http                          # http | rabbitmq | kafka
#    bind: "POST /channels/my-channel"        # http only: "<METHOD> <path>"
#    codec: "schemas/my-schema"               # archive-local codec id (a schemas/ folder) — decodes inbound bytes
#    cloudevents-mode: none                   # none | binary | structured
#    #idempotency-key-header: X-Message-Id    # dedupe key header (at-most-once intake)
#    #singleton: true                         # one active consumer across N replicas (leader-gated)
#    #broadcast: true                         # pub/sub fan-out instead of point-to-point
#    #max-concurrent-instances: 100           # concurrency cap
#    # Queue transports (rabbitmq/kafka) declare their broker wiring as flat keys, with
#    # credentials as ${ENV} secret references — never literal secrets in this file (the
#    # package-time validation rejects them; local demos may relax that at lint time):
#    #host: my-broker
#    #port: 5672
#    #username: ${BROKER_USERNAME}
#    #password: ${BROKER_PASSWORD}
#    #queue: my-queue
#    #prefetch-count: 1                       # deliver one message at a time
#    #ack-mode: on-complete                   # on-complete | on-persist
#    #auth:                                   # inbound auth on http binds
#    #  scheme: apikey                        # apikey | bearer | mtls
#    #  apikey:
#    #    value: ${MY_CHANNEL_API_KEY}        # ${ENV} reference, resolved at channel startup
#    #    header: X-Api-Key
