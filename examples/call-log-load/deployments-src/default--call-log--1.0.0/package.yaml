# Deployment package — one standalone authoring unit. `sutra package <this dir>` seals it
# into one content-addressed .sutra archive. Labels are opaque selectors: tenant is a label,
# not a resource scope.
labels:
  "module": "call-log"
  "tenant": "default"
  "version": "1.0.0"
engine:
  minContract: 1
