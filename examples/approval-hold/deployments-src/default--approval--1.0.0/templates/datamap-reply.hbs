{{!-- Reply template for the datamapping-showcase (declarative data-flow) flow. amount + riskBand are
   process variables written by the Seed/Derive FEEL data-assignment nodes; riskNote is the reply task's
   scoped <q:param> — overlaid on this render only, never persisted to instance state. --}}
<DataMapShowcase amount="{{amount}}" riskBand="{{riskBand}}" note="{{riskNote}}"/>